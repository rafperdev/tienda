import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; //Importando función desde .JS
import './index.css'; //Importando estilso .CSS

ReactDOM.render(
  <App />,
  document.getElementById('root')
);
